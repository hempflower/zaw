{
  "name": "@zaw/config",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "echo 'configuration package has no build output'",
    "typecheck": "echo 'configuration package has no TypeScript sources'",
    "lint": "echo 'configuration package has no lint target'",
    "test": "echo 'configuration package has no tests'"
  }
}
